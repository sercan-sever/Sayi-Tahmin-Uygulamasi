<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.info.sayitahminuygulamasi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sayi_tahmin_uygulamasi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sayi_tahmin_uygulamasi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SonucActivity"></activity>
        <activity android:name=".TahminActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>